# 03-Go仿真器详解：调度内核是如何工作的

> 本文档深入讲解 Go 仿真器的实现，包括 HTTP 服务、主仿真循环、Volcano 调度框架和状态管理。

---

## 一、整体结构

```
Volcano_simulator/
├── cmd/
│   └── sim/
│       └── main.go              # 唯一入口：HTTP + 主循环
├── pkg/
│   ├── scheduler/               # Volcano 调度框架
│   │   ├── api/                 # 数据类型（JobInfo、NodeInfo等）
│   │   ├── framework/           # 调度框架（Session、Action）
│   │   ├── actions/             # 调度动作（enqueue、allocate等）
│   │   ├── plugins/             # 调度插件（gang、drf等）
│   │   ├── cache/               # 调度缓存
│   │   └── util/                # 工具函数
│   └── simulator/
│       └── utils.go             # 仿真器辅助函数
└── vendor/                      # 第三方依赖（Volcano、K8s等）
```

---

## 二、main.go：大脑和心脏

### 2.1 全局状态

```go
var (
    // 集群状态（内存中）
    cluster = &schedulingapi.ClusterInfo{
        Nodes:          make(map[string]*schedulingapi.NodeInfo),
        Jobs:           make(map[schedulingapi.JobID]*schedulingapi.JobInfo),
        Queues:         make(map[schedulingapi.QueueID]*schedulingapi.QueueInfo),
        NamespaceInfo:  make(map[schedulingapi.NamespaceName]*schedulingapi.NamespaceInfo),
    }
    
    // 待提交任务队列（按提交时间排序）
    jobQueue = util.NewPriorityQueue(func(l, r interface{}) bool {
        return l.(*schedulingapi.JobInfo).SubTimestamp.Time.Before(
            r.(*schedulingapi.JobInfo).SubTimestamp.Time)
    })
    
    // 调度配置
    acts  []framework.Action    // 动作链
    tiers []conf.Tier           // 插件层级
    cfg   []conf.Configuration  // 插件配置
    
    // 控制标志
    loadNewSchedulerConf = true   // 是否有新配置
    notCompletion        = false  // 是否还有任务未完成
    restartFlag          = true   // 是否正在重启
    cnt                  = int64(0)      // 循环计数
    
    // 仿真时钟
    schedulingapi.NowTime = metav1.NewTime(time.Time{})
)
```

### 2.2 main() 函数流程

```go
func main() {
    // 1. 初始化默认队列和命名空间
    initDefaultQueueAndNamespace()
    
    // 2. 注册 Volcano 插件
    actions.InitV2()
    
    // 3. 启动 HTTP 服务（后台 goroutine）
    go server()
    
    // 4. 主仿真循环
    for true {
        // 等待重置完成或任务完成
        for !notCompletion || restartFlag {
            time.Sleep(200 * time.Millisecond)
        }
        
        // 提交到时间的 Job
        submitDueJobs()
        
        // 推进容器创建（Binding → Running）
        advanceContainerCreation()
        
        // 等待新的调度配置
        waitForSchedulerConf()
        
        // 执行调度
        runScheduling()
        
        // 同步 Pod 状态
        syncSimulationPodPhases()
        
        // 更新完成状态
        notCompletion = clusterHasBindingTask() || !jobQueue.Empty()
        
        // 时间 +1 秒
        schedulingapi.NowTime = metav1.NewTime(
            schedulingapi.NowTime.Add(time.Second))
        cnt++
    }
}
```

### 2.3 HTTP 服务（server()）

```go
func server() {
    http.HandleFunc("/reset", reset)           // 初始化
    http.HandleFunc("/step", step)             // 推进调度
    http.HandleFunc("/stepResult", stepResult) // 获取结果
    http.HandleFunc("/stepResultAnyway", stepResultAnyway)
    
    http.ListenAndServe(":8006", nil)
}
```

**为什么用 HTTP 而不是直接调用？**
- 解耦：Python 和 Go 可以独立重启
- 可调试：可以直接用 curl 测试
- 可扩展：未来支持远程仿真器

### 2.4 reset 处理器

**作用：** 初始化仿真环境

```go
func reset(w http.ResponseWriter, r *http.Request) {
    // 1. 如果还有任务在运行，先停止
    if notCompletion {
        restartFlag = true
        time.Sleep(1 * time.Second)
        jobQueue = NewPriorityQueue(...)  // 清空队列
    }
    
    // 2. 重置集群状态
    cluster = &schedulingapi.ClusterInfo{...}
    initDefaultQueueAndNamespace()
    
    // 3. 解析请求体
    body, _ := ioutil.ReadAll(r.Body)
    var workload simulator.WorkloadType
    json.Unmarshal(body, &workload)
    
    // 4. 加载节点
    nodes := simulator.Yaml2Nodes([]byte(workload.Nodes))
    for _, node := range nodes["cluster"] {
        nodeInfo := schedulingapi.NewNodeInfo(&node.Node)
        cluster.Nodes[nodeInfo.Name] = nodeInfo
        
        // 设置容器创建参数
        nodeInfo.CtnCreationTime = node.CtnCreationTime
        nodeInfo.CtnCreationExtraTime = node.CtnCreationExtraTime
        nodeInfo.CtnCreationTimeInterval = node.CtnCreationTimeInterval
    }
    
    // 5. 加载任务
    jobs := simulator.Yaml2Jobs([]byte(workload.Workload))
    for _, job := range jobs["jobs"] {
        jobInfo := schedulingapi.NewJobInfoV2(job)
        
        // 设置提交时间（从 label 中解析）
        if subTime, ok := job.Labels["sub-time"]; ok {
            timestamp, _ := strconv.Atoi(subTime)
            jobInfo.SubTimestamp = metav1.NewTime(
                time.Time{}.Add(time.Duration(timestamp) * time.Second))
        }
        
        jobQueue.Push(jobInfo)
    }
    
    notCompletion = true
    restartFlag = false
    
    // 6. 返回确认
    info := simulator.Info{Done: false, ...}
    resp, _ := json.Marshal(info)
    w.Write(resp)
}
```

### 2.5 step 处理器

**作用：** 接收调度配置，触发调度

```go
func step(w http.ResponseWriter, r *http.Request) {
    body, _ := ioutil.ReadAll(r.Body)
    var scheduler_conf simulator.ConfType
    json.Unmarshal(body, &scheduler_conf)
    
    // 解析调度配置
    acts, tiers, cfg, _ = scheduler.UnmarshalSchedulerConfV2(scheduler_conf.Conf)
    
    // 标记有新配置（主循环会执行调度）
    loadNewSchedulerConf = true
    
    w.Write([]byte("1"))  // 返回成功
}
```

### 2.6 stepResult 处理器

**作用：** 返回当前仿真状态

```go
func stepResult(w http.ResponseWriter, r *http.Request) {
    // 如果还在调度中，返回 "0" 表示未就绪
    if loadNewSchedulerConf && notCompletion {
        w.Write([]byte("0"))
        return
    }
    
    // 构建返回数据
    info := simulator.Info{
        NotCompletion: notCompletion,
        Nodes:         cluster.Nodes,
        Jobs:          cluster.Jobs,
        Clock:         schedulingapi.NowTime.Local().String(),
    }
    
    resp, _ := json.Marshal(info)
    w.Write(resp)
}
```

---

## 三、主仿真循环详解

### 3.1 提交到时间的 Job

```go
func submitDueJobs() {
    for !jobQueue.Empty() {
        front := jobQueue.Pop().(*schedulingapi.JobInfo)
        
        // 如果还没到提交时间，放回队列
        if schedulingapi.NowTime.Time.Before(front.SubTimestamp.Time) {
            jobQueue.Push(front)
            break
        }
        
        // 提交进集群
        cluster.Jobs[front.UID] = front
        
        // 设置 Pod 创建时间
        for _, task := range front.Tasks {
            task.Pod.SetCreationTimestamp(schedulingapi.NowTime)
        }
    }
}
```

### 3.2 推进容器创建

**为什么要模拟容器创建？**

真实的 K8s 中，调度器把 Pod 绑定到节点后，kubelet 需要拉取镜像、创建容器，这个过程需要时间。仿真器也要模拟这个延迟。

```go
func advanceContainerCreation() {
    // 1. 递减所有 Binding Task 的倒计时
    for _, node := range cluster.Nodes {
        for _, task := range node.Tasks {
            if task.Status == schedulingapi.Binding {
                task.CtnCreationCountDown -= 1
            }
        }
    }
    
    // 2. 对于每个节点，按间隔将符合条件的 Binding Task 改为 Running
    for _, node := range cluster.Nodes {
        // 检查是否到了创建间隔
        if cnt % node.CtnCreationTimeInterval != 0 {
            continue
        }
        
        // 找到最早创建的、倒计时为 0 的 Binding Task
        var earliestTask *schedulingapi.TaskInfo
        for _, task := range node.Tasks {
            if task.Status == schedulingapi.Binding && task.CtnCreationCountDown <= 0 {
                if earliestTask == nil || 
                   task.Pod.CreationTimestamp.Before(&earliestTask.Pod.CreationTimestamp) {
                    earliestTask = task
                }
            }
        }
        
        // 改为 Running 状态
        if earliestTask != nil {
            earliestTask.Status = schedulingapi.Running
            earliestTask.Pod.Status.Phase = v1.PodRunning
            earliestTask.Pod.Status.StartTime = schedulingapi.NowTime.DeepCopy()
        }
    }
}
```

**参数说明：**
- `CtnCreationTime`：基础创建时间（秒）
- `CtnCreationExtraTime`：额外随机时间（秒）
- `CtnCreationTimeInterval`：同一节点上容器创建的间隔（秒）

### 3.3 执行调度

```go
func runScheduling() {
    // 打开调度会话
    ssn := framework.OpenSessionV2(cluster, tiers, cfg)
    
    // 按顺序执行动作链
    for _, action := range acts {
        action.Execute(ssn)
    }
    
    // 关闭会话（释放资源）
    // framework.CloseSession(ssn)  // 当前有 bug 未启用
}
```

---

## 四、Volcano 调度框架

### 4.1 框架三层结构

```
┌─────────────────────────────────────────────────────────┐
│                    Action 层                            │
│  定义调度的"执行流程"                                    │
│  如：先 enqueue，再 allocate，最后 backfill             │
├─────────────────────────────────────────────────────────┤
│                    Plugin 层                            │
│  定义调度的"决策策略"                                    │
│  如：用 gang 插件做 Gang 调度                            │
├─────────────────────────────────────────────────────────┤
│                   Framework 层                          │
│  提供调度的"基础设施"                                    │
│  Session、Statement、Cache 等                            │
└─────────────────────────────────────────────────────────┘
```

### 4.2 Session：调度上下文

**Session 是一次调度的"工作空间"：**

```go
type Session struct {
    UID        uuid.UUID
    
    // 集群快照
    Nodes  []*NodeInfo
    Jobs   map[JobID]*JobInfo
    Queues map[QueueID]*QueueInfo
    
    // 插件注册的回调函数
    NamespaceOrderFns []CompareFn
    QueueOrderFns     []CompareFn
    JobOrderFns       []CompareFn
    TaskOrderFns      []CompareFn
    PredicateFns      map[string]PredicateFn
    NodeOrderFns      map[string]NodeOrderFn
    
    // 本次调度的操作记录
    Statements []*Statement
}
```

**为什么需要 Session？**
- **隔离性**：每次调度独立，互不干扰
- **一致性**：基于快照调度，避免状态变化导致不一致
- **可回滚**：Statement 记录操作，失败可以回滚

### 4.3 Statement：操作记录

**Statement 记录一次调度操作，支持回滚：**

```go
type Statement struct {
    operations []Operation  // 操作列表
}

type Operation interface {
    Execute()  // 执行
    Rollback() // 回滚
}

// 示例：分配操作
type AllocateOperation struct {
    task *TaskInfo
    node *NodeInfo
}

func (op *AllocateOperation) Execute() {
    op.node.AddTask(op.task)
    op.task.NodeName = op.node.Name
}

func (op *AllocateOperation) Rollback() {
    op.node.RemoveTask(op.task)
    op.task.NodeName = ""
}
```

### 4.4 Action：调度动作

**Action 是调度的"执行阶段"：**

| Action | 作用 | 类比 |
|--------|------|------|
| `enqueue` | 将符合条件的 Job 加入调度队列 | 报名参赛 |
| `allocate` | 为待调度的 Task 分配节点 | 分配座位 |
| `preempt` | 抢占低优先级任务的资源 | 抢座位 |
| `reclaim` | 回收队列间的不公平份额 | 重新分配 |
| `backfill` | 回填小任务到空闲资源 | 插空坐 |
| `elect` | 选举保留资源的目标任务 | 预留座位 |

**allocate 动作的核心逻辑：**

```go
func (alloc *Action) Execute(ssn *framework.Session) {
    // 1. 收集待调度任务
    var jobs []*JobInfo
    for _, job := range ssn.Jobs {
        if job.IsPending() && ssn.JobValid(job) {
            jobs = append(jobs, job)
        }
    }
    
    // 2. 排序（按优先级、提交时间等）
    sortJobs(jobs, ssn.JobOrderFns)
    
    // 3. 为每个任务分配节点
    for _, job := range jobs {
        for _, task := range job.Tasks {
            // 找到合适的节点
            node := selectNode(task, ssn.Nodes, ssn.PredicateFns, ssn.NodeOrderFns)
            
            if node != nil {
                // 记录分配操作
                ssn.Statement.Allocate(task, node)
            }
        }
    }
    
    // 4. 提交所有操作（或回滚）
    ssn.Statement.Commit()
}
```

### 4.5 Plugin：调度策略

**Plugin 是调度的"决策逻辑"：**

```go
type Plugin interface {
    Name() string
    
    // 排序函数
    NamespaceOrderFn() CompareFn
    QueueOrderFn() CompareFn
    JobOrderFn() CompareFn
    TaskOrderFn() CompareFn
    
    // 过滤函数
    PredicateFn() PredicateFn
    
    // 评分函数
    NodeOrderFn() NodeOrderFn
}
```

**常用插件：**

| 插件 | 功能 | 使用场景 |
|------|------|----------|
| `gang` | Gang 调度：一组任务要么全启动，要么全不启动 | 分布式训练 |
| `drf` | 主导资源公平：按主导资源份额公平分配 | 多租户公平性 |
| `proportion` | 比例分配：按队列权重分配资源 | 队列资源配比 |
| `binpack` | 紧凑打包：尽量填满节点 | 提高利用率 |
| `nodeorder` | 节点排序：按资源、标签等排序 | 节点选择策略 |
| `predicates` | 谓词过滤：检查节点是否满足条件 | 硬性约束检查 |

**gang 插件示例：**

```go
func (gp *gangPlugin) JobValid(job *JobInfo) *ValidationResult {
    // Gang 调度要求：任务数 >= MinAvailable
    if len(job.Tasks) < job.MinAvailable {
        return &ValidationResult{
            Pass:    false,
            Reason:  "NotEnoughTasks",
            Message: "任务数不足",
        }
    }
    return &ValidationResult{Pass: true}
}

func (gp *gangPlugin) TaskOrderFn() CompareFn {
    // Gang 内的任务优先级相同，按提交时间排序
    return func(l, r interface{}) bool {
        return l.(*TaskInfo).Pod.CreationTimestamp.Before(
            &r.(*TaskInfo).Pod.CreationTimestamp)
    }
}
```

---

## 五、状态管理

### 5.1 Pod 状态机

```
Pending ──→ Pipelined ──→ Binding ──→ Running
  │              │            │           │
  │              │            │           │
  └──────────────┴────────────┴───────────┘
         （仿真中简化为：Pending → Running）
```

**各状态含义：**
- `Pending`：等待调度
- `Pipelined`：已预留资源，等待 Gang 条件满足
- `Binding`：已绑定节点，正在创建容器
- `Running`：容器已就绪，任务运行中

### 5.2 状态同步

```go
func syncSimulationPodPhases() {
    for _, job := range cluster.Jobs {
        for _, task := range job.Tasks {
            switch task.Status {
            case schedulingapi.Pending,
                 schedulingapi.Pipelined,
                 schedulingapi.Binding:
                task.Pod.Status.Phase = v1.PodPending
            default:
                task.Pod.Status.Phase = v1.PodRunning
            }
        }
    }
}
```

**对外简化：**
- 调度中（Pending/Pipelined/Binding）→ 对外显示 Pending
- 已运行（Running）→ 对外显示 Running

这样 Python 客户端只需要处理两种状态，简化逻辑。

---

## 六、关键设计决策

### 6.1 为什么用离散时间（1秒粒度）？

| 方案 | 优点 | 缺点 | 选择 |
|------|------|------|------|
| 连续时间 | 精确 | 复杂，难以控制 | ❌ |
| 离散时间（1秒） | 简单可控 | 精度有限 | ✅ |
| 事件驱动 | 高效 | 实现复杂 | ❌ |

**结论：** 对于调度策略验证，1 秒粒度足够，且实现简单。

### 6.2 为什么全内存存储？

| 方案 | 优点 | 缺点 | 选择 |
|------|------|------|------|
| 内存 | 快速，简单 | 重启丢失 | ✅ |
| 数据库 | 持久化 | 复杂，有依赖 | ❌ |
| 文件 | 持久化 | 慢 | ❌ |

**结论：** 仿真系统不需要持久化，重启即重置符合实验需求。

### 6.3 为什么用 HTTP 而不是 gRPC？

| 方案 | 优点 | 缺点 | 选择 |
|------|------|------|------|
| HTTP/REST | 简单，易调试 | 性能略差 | ✅ |
| gRPC | 性能好，类型安全 | 复杂，依赖多 | ❌ |
| 直接调用 | 最快 | 耦合严重 | ❌ |

**结论：** HTTP 足够满足需求，且便于调试（可用 curl）。

---

## 七、总结

Go 仿真器的核心设计：

```
HTTP 接口（接受控制指令）
      ↓
主仿真循环（时间推进 + 事件处理）
      ↓
Volcano 框架（真实调度代码）
      ↓
内存状态（虚拟集群）
```

**关键特点：**
1. **真实**：运行真实的 Volcano 代码，不是简化版
2. **可控**：离散时间，每步可调
3. **可观测**：详细的状态返回
4. **轻量**：纯内存，单机运行

---

准备好深入了解 FlexNPU 资源模型了吗？请继续阅读 `04-FlexNPU详解.md`！
