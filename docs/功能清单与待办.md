# 功能清单与待办（TODO）

已与当前仓库代码对照核对。细节见 [**requirements.md**](./requirements.md)、[**architecture.md**](./architecture.md)。

**图例：** **`[x]`** 已完成  **`[ ]`** 未完成或可选增强（第四节「非目标」不用勾选，避免与待办混淆）

---

## 仿真器（Go）

- [x] HTTP（默认 8006）：`/reset`、`/step`、`/stepResult`、`/stepResultAnyway`
- [x] 仿真时钟按秒推进，Job 按 `SubTimestamp` 入队并提交进集群
- [x] Job 的 `metadata.labels` 中 `sub-time`（秒）用于提交/创建时刻
- [x] 提交进集群时为各 Pod 写 `CreationTimestamp`；Binding→Running 时写 `status.startTime`
- [x] `OpenSessionV2` + 配置里的 actions 顺序执行；同步对外 Pod Phase（含 Pipelined→Pending 等）
- [x] 由 batch Job YAML 生成 Task/Pod（**仅使用 `Tasks[0]`** 的模板与副本数）
- [x] 合并 `Task.Template.Annotations` 到 Pod，再写 group 注解
- [x] 节点支持容器创建耗时、计算速度等 `V2Node` 扩展字段及默认值

## Python 客户端

- [x] `SimRun`：`reset` → `step` → 轮询 `stepResult`（占位时响应体为数字 `0`，`json.loads` 得到 **`int`**，与字典快照类型不同，靠 `str(resultdata) == '0'` 判断）
- [x] `JsonHttpClient`：请求 + `json.loads`、失败重试
- [x] 注入 `npuGranularityPercent` 与 workload 一致，供 FlexNPU 分卡估算
- [x] 加载 cluster / workload / plugins，输出仿真器约定 YAML 字符串
- [x] flexnpu_core 可选粒度上取整，memory 不取整；写原始 core 注解供利用率统计
- [x] FlexNPU：节点按卡容量、`flexnpu-num`、Running/Binding Pod，轮询分卡；core 利用率/分配双轨
- [x] 落盘：`tasksSUM.csv`、`pod_phase_count.txt`、`flexnpu_utilization.txt`、四类 CSV（含 `submit_time` / `status.startTime`、Node 比率无 `%` 等）

## 文档与合规

- [x] `README.md`、`docs/requirements.md`、`docs/architecture.md`、`input_config/README.md`、`LICENSE`、`许可说明.md`

## 未完成或可选增强

- [ ] `figures/` 作图：未接入 `SimRun` 主流程（`draw_pod_figures` 等仍注释）
- [ ] 仿真结束打印的 **JCT** 表为占位，未按真实完成时间计算
- [ ] 多 `tasks[]` 模板：仅展开第一个 task，完整支持需改 `NewJobInfoV2` 等
- [ ] `main.go` 中 `TaskStatusIndex`、主循环内注释 todo 等未收尾
- [ ] 若要将 `stepResult` 占位改为统一 JSON 信封，需改仿真器与客户端协议（当前依赖整数 `0`）

## 非目标（不排期）

以下**不是**「待开发勾选」，仅为范围说明：

- 对接真实 Kubernetes / Volcano 集群  
- 在 Python 侧改写调度算法内核  
- 生产级高可用、多租户隔离  

---

条目或行为变更时，请同步更新本文档及 `requirements.md` / `architecture.md`。
