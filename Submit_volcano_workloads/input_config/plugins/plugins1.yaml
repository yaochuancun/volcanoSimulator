apiVersion: v1
kind: PluginList
scheduler:
  actions: "enqueue, allocate"
  tiers:
    - plugins:
      - name: drf
        enablePreemptable: false
      - name: predicates
      - name: nodeorder
output:
  outDir: ./result/{date}
  formats:
    - csv
